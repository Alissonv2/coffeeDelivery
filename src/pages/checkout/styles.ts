import styled from 'styled-components'

export const ContainerCheckout = styled.main`
	display: flex;
	flex-wrap: wrap;
	width: 100%;
	justify-content: space-between;
	padding-top: 3rem;
	gap: 3rem;
`

export const AddressAndOrdersSection = styled.section`
	display: flex;
	flex-direction: column;
	flex: 1;

	h1 {
		font-family: 'Baloo 2';
		font-style: normal;
		font-weight: 700;
		font-size: 1.5rem;
		line-height: 130%;
		color: #403937;
	}
`
